[project]
name = "1stproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lexaphantom-dot", email = "323722750+lexaphantom-dot@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "ruff>=0.16.6",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
1stproject = "1stproject:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
